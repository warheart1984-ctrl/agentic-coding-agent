# Agentic Coding Agent Architecture

The agent is a governed CRK-2 implementation with CLI, local inference, governance controls, and Cockpit surfaces. It is a replaceable capability consumer of the frozen Constitutional AI Runtime; it does not own authority. CRK-3 remains roadmap work and may not expand the frozen runtime core without constitutional review.
