# Deployment Guide

Requires Node 18 or newer. Install dependencies, run `npm run build`, then use the compiled CLI or `npm run run:local-governed`. This is a local prototype deployment path, not a production certification.
